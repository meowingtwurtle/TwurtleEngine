#pragma once

#include <vector>

#include <glm/glm.hpp>

namespace randomcat::engine::graphics::detail {
    struct default_vertex {
        glm::vec3 location;
    };
}    // namespace randomcat::engine::graphics::detail
